export interface IEmployee {
  Title: string
  isnomresi: string
  TeskilatVahidi: string
  StatVezifesi: string
  KoperatifMail: string
  birthday: string
  profilePictureUrl?: string // Optional property to store the profile picture URL
}
